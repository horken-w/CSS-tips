(function(document, window){
	var textarea = Array.prototype.slice.call(document.getElementsByTagName('textarea'));

	var addEvent = function(action, object, callback){

	}

	function textRemain(domEl){
		domEl.forEach(function(i, v){
			var max = i.maxLength;

			i.addEventListener
		})
	}
	textRemain(textarea);
})(document, window)