import { ImageCroperDirective } from './image-croper.directive';

describe('ImageCroperDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCroperDirective();
    expect(directive).toBeTruthy();
  });
});
